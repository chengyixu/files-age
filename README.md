# @chengyixu/files-age

A zero-dependency CLI for listing file modification ages.

```sh
npx @chengyixu/files-age --sort --dir .
```

Options: `--sort` lists newest first; `--reverse` lists oldest first; `--dir <dir>` lists a directory; `--json` emits scripting-friendly JSON.
